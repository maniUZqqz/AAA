اول تأیید کنم کد واقعی است: check.py هر ۱۹ سنجه را OK داد، و تست‌ها را خودم اجرا کردم — ۲۱۴ تست سبز، ۴ اسکیپ (همه‌ی اسکیپ‌ها تست FFmpeg‌اند چون روی این سیستم نصب نیست). در کل بک‌اند و فرانت فقط یک NotImplementedError هست و آن هم کلاس پایه‌ی TTS است. یعنی ادعای «ساخته شده، نه یک ایده» درست است.

حالا چیزی که کم است — به ترتیب اهمیت:

۱. چیزهایی که اصلاً یک خط کد ندارند
درگاه پرداخت — صفر. جستجو برای zarinpal/idpay/nextpay/shaparak در کل کد و سایت: هیچ. ارائه این را با شدت «متوسط» فهرست کرده، ولی اثرش بزرگ‌تر است: Subscription.renew() وضعیت را از TRIALING به PAST_DUE می‌برد و هیچ مسیری برای ACTIVE شدن وجود ندارد جز ویرایش دستی در ادمین. کل مدل درآمدی — ۱۸۳ مشتری تا سر به سر — روی کارت‌به‌کارت و ادیت دستی می‌چرخد. طنزش این است که محصول برای مشتریِ مشتری چرخه‌ی رسید و تأیید انسانی ساخته، ولی برای اشتراک خودش نساخته.

هیچ چیزی برای دیپلوی وجود ندارد. نه Dockerfile، نه docker-compose، نه nginx، نه systemd، نه Procfile، نه اسکریپت بکاپ. محصول از start.bat روی یک ویندوز دسکتاپ بالا می‌آید. فاز ۱۷ می‌گوید «دور اول انجام شد، مانده بکاپ/دیپلوی» — ولی دور اولی وجود ندارد؛ هیچ آرتیفکتی نیست.

دیتابیس SQLite هاردکد شده. settings.py:127 هیچ شاخه‌ای برای Postgres ندارد. SQLite قفل تک‌نویسنده دارد و قرار است ۲۸۳ فروشگاه با ورکرهای Celery رویش بنویسند.

حلقه‌ی یادگیری بسته نیست. فلسفه‌ی محصول در ROADMAP این است: ... → PUBLISHING → ANALYTICS → LEARNING. ولی analytics/services.py فقط شمارش داخلی می‌کند (گفتگو، سفارش، درخواست AI، کمپین). هیچ داده‌ی عملکردی از اینستاگرام برنمی‌گردد — نه بازدید، نه لایک، نه ریچ. پس بند ۱۱ چشم‌انداز («یادگیری از نتایج برای بهبود تصمیم‌های بعدی») هیچ مسیر داده‌ای ندارد. حلقه باز است.

Django Channels/WebSocket در جدول استک ROADMAP هست ولی در کد نیست؛ asgi.py خالی است و فرانت هر ۲.۵ ثانیه polling می‌کند. (انصافاً: data.json این را ادعا نکرده — فقط ROADMAP کهنه است.)

۲. جایی که کد با ادعا می‌خواند ولی با سخت‌افزار نه
مدل ظرفیت، زمان LLM را صفر گرفته. بخش render فقط ویدیو (۴ دقیقه بر ثانیه) و پوستر (۲ دقیقه) را می‌شمارد، با یادداشت «کپشن روی GPU نیست». ولی qwq:32b و qwen3-vl:30b روی همان GPU‌اند — داکسترینگ خودِ services/gpu.py می‌گوید این‌ها با FLUX/Wan سر یک کارت دعوا دارند و باید دور هر کار آنلود شوند. زمان هر سواپ و لود مدل در آن ۲۰۳ دقیقه‌ای که «۲۸۳ مشتری» از آن درمی‌آید اصلاً نیست.

qwq:32b روی RTX 3060 12GB جا نمی‌شود. در لاگ خودِ beter.md: GPULayers:29(35..63) — از ۶۴ لایه فقط ۲۹ تا روی GPU، بقیه روی CPU. همین باعث شد «تحلیل رقبا» ارور بدهد. یعنی برنامه‌ی سخت‌افزاری ۶۰۰ میلیون تومانی حول مدل‌هایی بسته شده که روی آن کارت جا نمی‌شوند، و نقطه‌ی سر به سر روی همین می‌نشیند.

«چند دقیقه» در FAQ مشتری با اعداد خودشان نمی‌خواند. پکیج حرفه‌ای = ۴۵ ثانیه ویدیو × ۴ دقیقه = ۱۸۰ دقیقه، به‌علاوه ۱۵ پوستر × ۲ = ۳۰ دقیقه → ۳.۵ ساعت GPU. حتی یک ویدیوی ۵ ثانیه‌ای ۲۰ دقیقه است. جواب FAQ («یک بسته‌ی کامل محتوا در چند دقیقه آماده می‌شود») را همان data.json نقض می‌کند.

۳. دو سوراخ در قول «داده‌ی شما پیش ما می‌ماند»
کارت اعتماد مشتری می‌گوید «پردازش روی سرور خودمان انجام می‌شود، نه سرویس خارجی». ولی:

tts.py:34 خودش نوشته needs internet — edge-tts سرویس مایکروسافت است. متن نریشن هر ویدیوی هر مشتری به مایکروسافت می‌رود.
ModelProvider هیچ فارین‌کی به Store ندارد — تنظیم سراسری است. اپراتور می‌تواند متن/بینایی/تصویر/ویدیو را برای کل پلتفرم به یک API بیرونی سوییچ کند و فروشگاه‌دار نه می‌بیند، نه رضایت می‌دهد، نه تنظیم مخصوص خودش دارد.
۴. مسیر مشتری
انتخاب پکیج در درگاه ورودی گم می‌شود. pricing/page.tsx:79 به /register?plan={slug} لینک می‌دهد (۱۳ دکمه CTA در کل سایت)، ولی هیچ‌جای فرانت searchParams را نمی‌خواند. کاربر «حرفه‌ای» را انتخاب می‌کند و در صفحه‌ی ثبت‌نام خالی می‌افتد.
صفحه‌ی تماس فرم ندارد — نه action، نه fetch. مسیر جذب لید که GTM رویش حساب کرده وجود ندارد.
هر فروشگاه فقط یک مالک — Store.owner یک FK ساده است. تیم و کاربر دوم نیست؛ صاحب مغازه و آدم مارکتینگش باید یک پسورد را شریک شوند.
۵. لیست ریسک خودشان به جای اشتباه نشانه رفته
gaps در data.json می‌گوید «سایت عمومی و ثبت‌نام آنلاین هنوز ساخته نشده» با شدت high — ولی website/ یک سایت کامل Next.js 15 با SEO و وبلاگ و sitemap است و /register هم در پنل هست. این شکاف بسته شده و کهنه مانده. در عوض سه شکاف واقعیِ بالا (دیپلوی، پرداخت، ظرفیت) با آن شدت در لیست نیستند. یعنی اسلاید ریسکی که سرمایه‌گذار می‌بیند به جای اشتباه اشاره می‌کند.

نکته‌ی مربوط: check.py عدد ۲۱۵ را با شمردن متنی def test_ درمی‌آورد؛ واقعاً ۲۱۴ تا اجرا می‌شود. خودش بی‌اهمیت است، ولی نشان می‌دهد هر ۱۹ سنجه grep دنبال یک رشته‌اند — وجود را چک می‌کنند نه رفتار. هیچ‌کدام از موارد بخش ۱ تا ۴ را نمی‌توانستند بگیرند.

اگر بخواهی این را به شکل یک صفحه‌ی مرتب و قابل‌اشتراک با اولویت‌بندی دربیاورم بگو تا بسازم — یا می‌توانم بروم سراغ بستن یکی از این‌ها؛ به نظرم ?plan= و فرم تماس یک‌ساعته‌اند، ولی ترتیب درست کار احتمالاً همان فاز ۰.۵ است: یک تیزر واقعی بساز تا معلوم شود عدد «۴ دقیقه بر ثانیه» و کیفیت خروجی اصلاً درست است یا نه، چون کل مدل مالی روی آن ایستاده.