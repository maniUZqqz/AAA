فایل تسک‌های **انجام‌نشده**. هرچه تمام شد از اینجا حذف می‌شود.

---

# توسعه از روی رودمپ

رودمپ از `ایده‌ها پنل.md` به‌روز شد (هر ۱۵ قابلیت پوشش داده شد، ۴ فاز جدید اضافه شد).
آنچه مانده، ساختن خود فازهاست.

جزئیات و DoD هر فاز: [`Code/UpMarket/ROADMAP.md`](Code/UpMarket/ROADMAP.md) بخش ۱۱.

| فاز | مانده |
|---|---|
| **۰.۵ — بنچمارک واقعی** | **دروازه** — تا این نشود، ظرفیت و قیمت‌گذاری حدس‌اند |
| **۱۸ — Production Foundation** | فقط دو مورد: Object Storage (تا فاز ۲۳ لازم نیست) و Monitoring |
| ۲۰ — Generation Pipeline | تولید تدریجی، Scene State، دو Mode، WebSocket |
| ۲۱ — AI Quality Engine | **مزیت فنی اصلی محصول** — Prompt Compiler، Vision QC، تعمیر سه‌سطحی |
| ۲۲ — Audio Engine | چند-Provider، Audio QC، Phoneme Timing |
| ۲۳ — Editing Engine | EDL، Auto Cut، Tool Registry، ادیتور ایجنتیک |
| ۲۴ — Omnichannel | Inbox واحد، Adapterها، زمان‌بندی، کمپین به‌عنوان مدیر وظایف |
| ۲۵ — Learning Engine | داده‌ی عملکرد از پلتفرم‌ها، Funnel، حلقه‌ی بسته |
| ۲۶ — GTM | CRM، Referral، Affiliate، Marketing Intelligence |
| ۲۸ — هوش فروشگاه | قیمت تمام‌شده، تحلیل سود، ترندها |
| ۲۹ — پلتفرم API | احراز هویت، سهمیه، ۵ API عمومی |
| ۳۰ — بازاریابی تلفنی | **معلق تا حل دو قید حقوقی** (ضبط تماس، جمع‌آوری اطلاعات شخصی) |

## نکته‌های ترتیب

**فاز ۰.۵ دروازه است.** سرعت رندر اندازه‌گیری نشده و بازه‌اش ۳ تا ۹ دقیقه بر ثانیه
است. در حالت کند سقف ظرفیت ۱۳۶ مشتری می‌شود — **زیر سر به سر پایدار (۱۹۰)**.

**فاز ۱۸ تقریباً تمام است.** PostgreSQL، داکر، Nginx، بکاپ، گاردهای production،
AI Gateway و Data Policy همه انجام شدند. آنچه مانده دو موردی است که هیچ‌کدام
الان لازم نیستند: Object Storage (وقتی موتور تدوین فاز ۲۳ بیاید) و Monitoring.

**فاز ۱۹ کامل شد.** پرداخت، اعتبار با تضمین کیفیت، چندکاربره با شش نقش،
Onboarding و تنظیمات ایجنت فروش — همه ساخته و با اجرای واقعی آزمایش شدند.
پس **فاز ۲۰ نقطه‌ی شروع بعدی است.**

**یک نکته که باید روی سرور انجام شود:** داکر روی این سیستم نصب نیست، پس
اولین `docker compose up` هنوز اجرا نشده. YAML و تنظیمات تأیید شده‌اند، ولی
ساخته شدن ایمیج‌ها نه.

## بلوکرهای بیرونی (کار مهندسی ندارند)

- `/case-studies`، `/customers`، `/testimonials` — منتظر **اولین مشتری واقعی**
- `/resources` — منتظر محتوای نوشته‌شده
- فاز ۳۰ — منتظر تصمیم حقوقی
